# v0.1.15

## 示例与文档

- 精简工作台后端的管理路由、错误处理、状态读写与请求中间件，保留完整示例能力、管理接口和状态格式，降低阅读与二次开发成本。
- 重写仓库 README 和工作台说明，集中安装、开发与打包入口，补充按功能阅读源码的导航，并沿用主仓图标。
- 补充管理路由、任务状态、文本读取与请求中间件的回归测试。

## 开发支持

- 组件库升级到 `@codex-proxy/ui` v0.3.0，使用其公开 Vite 适配器统一普通构建与源码联调配置；锁文件固定实际提交。
- 正式 SDK 与打包器仍固定到 `f770ba127d1293bb482921019e61cae7cb3b7de9`，独立检出无需依赖本地宿主或 UI 源码。

## 升级说明

- 本版按示例仓库约定发布为预发行，安装时指定 `v0.1.15` 并允许预发行；不会自动升级或启用运行实例。
- 宿主通信协议保持 v1，与 v0.1.13 的要求一致。提供 Linux x86_64、Linux aarch64、macOS aarch64 安装包及 SHA-256 校验文件。
